# Nutri-Datahub
Nutri is a nutrition-label generator for DataHub catalogs: point it at any dataset, dashboard, or model and it produces a scannable facts panel that it writes back into DataHub, so every asset in your catalog gets a glanceable health label the next person or agent can trust without recomputing it.
